(function () {
    var message = 'This JavaScript was loaded from an unsigned file.';
    message += ' Since a signature was declared for this file, it should not have loaded.';
    message += ' If you see this message, something went wrong.';
    console.log(message);
}());
